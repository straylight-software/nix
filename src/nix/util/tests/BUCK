# // straylight // nix // util // tests
#
# Unit and property-based tests for libutil
#
# Dependencies are now handled via Buck2's dependency system.
# All transitive deps come from //third_party with proper static linking.

# Common test dependencies - util's deps are handled via exported_deps
UTIL_TEST_DEPS = [
    "//src/nix/util:util",
    "//third_party:catch2",
    "//third_party:rapidcheck",
]

UTIL_TEST_COMPILER_FLAGS = [
    "-std=c++23",
]

# Minimal linker flags for static linking (no hardcoded paths)
UTIL_LINKER_FLAGS = [
    # pthread for boost::thread
    "-lpthread",
]

# ─────────────────────────────────────────────────────────────────────────────
# base-n encoding/decoding tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "base-n_test",
    srcs = ["base-n_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# checked arithmetic tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "checked-arithmetic_test",
    srcs = ["checked-arithmetic_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# canonical path tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "canon-path_test",
    srcs = ["canon-path_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# LRU cache tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "lru-cache_test",
    srcs = ["lru-cache_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# string utilities tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "strings_test",
    srcs = ["strings_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# topological sort tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "topo-sort_test",
    srcs = ["topo-sort_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# URL parsing tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "url_test",
    srcs = ["url_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# URL ada comparison tests (boost::url vs ada WHATWG)
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "url_ada_comparison_test",
    srcs = ["url_ada_comparison_test.cpp"],
    deps = UTIL_TEST_DEPS + [
        "//third_party:ada",
    ],
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# NAR archive format tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "archive_test",
    srcs = ["archive_test.cpp"],
    deps = UTIL_TEST_DEPS + [
        "//src/straylight/nix/testing:testing",
    ],
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# JSON utilities tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "json-utils_test",
    srcs = ["json-utils_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# Resource pool tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "pool_test",
    srcs = ["pool_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# Resource pool concurrency tests (isolated - may hang, debugging needed)
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "pool_concurrency_test",
    srcs = ["pool_concurrency_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# hash utilities tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "hash_test",
    srcs = ["hash_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# source path tests (property-based and fuzz tests)
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "source-path_test",
    srcs = ["source-path_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# serialisation tests (property-based and fuzz tests)
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "serialise_test",
    srcs = ["serialise_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# process handling tests (ECHILD race conditions, concurrent wait/kill)
# Tests for the bug: "cannot get exit status of PID: No child process"
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "processes_test",
    srcs = ["processes_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# signal handling tests (SIGTSTP, suspend callbacks, signal restoration)
# Tests for signal handler registration and callback mechanisms
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "signals_test",
    srcs = ["signals_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# logging synchronization tests (concurrent output, interleaving prevention)
# Tests for issues #7298 and #14294 - concurrent log output synchronization
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "logging_test",
    srcs = ["logging_test.cpp"],
    deps = UTIL_TEST_DEPS,
    compiler_flags = UTIL_TEST_COMPILER_FLAGS,
    linker_flags = UTIL_LINKER_FLAGS,
    link_style = "static",
)
