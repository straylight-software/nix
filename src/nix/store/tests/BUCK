# // straylight // nix // store // tests
#
# Unit tests for libstore

STORE_TEST_DEPS = [
    "//src/nix/store:store",
    "//src/nix/util:util",
    "//vendor:catch2",
    "//vendor:rapidcheck",
]

STORE_TEST_COMPILER_FLAGS = [
    "-std=c++23",
]

STORE_LINKER_FLAGS = [
    "-lpthread",
]

# ─────────────────────────────────────────────────────────────────────────────
# authorization settings tests
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "authorization-settings_test",
    srcs = ["authorization-settings_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# schema compatibility tests - P0 database compatibility
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "schema-compatibility_test",
    srcs = ["schema-compatibility_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# protocol compatibility tests - P0 worker protocol compatibility
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "protocol-compatibility_test",
    srcs = ["protocol-compatibility_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# store path format tests - P0 store path compatibility with upstream nix
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "store-path-format_test",
    srcs = ["store-path-format_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# hash format compatibility tests - P0 hash format compatibility with upstream nix
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "hash-format_test",
    srcs = ["hash-format_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# narinfo format tests - binary cache interop compatibility
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "narinfo-format_test",
    srcs = ["narinfo-format_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# derivation format tests - ATerm derivation format compatibility
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "derivation-format_test",
    srcs = ["derivation-format_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# store path benchmarks - performance tests for store path operations
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "store-path_bench",
    srcs = ["store-path_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# hash benchmarks - performance of hash operations on every store path access
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "hash_bench",
    srcs = ["hash_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# narinfo benchmarks - critical path performance for binary cache fetches
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "narinfo_bench",
    srcs = ["narinfo_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# NAR benchmarks - core store path content handling performance
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "nar_bench",
    srcs = ["nar_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# derivation benchmarks - parsing/serialization performance
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "derivation_bench",
    srcs = ["derivation_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# SQLite benchmarks - hot path performance for local store operations
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "sqlite_bench",
    srcs = ["sqlite_bench.cpp"],
    deps = [
        "//vendor:catch2",
        "//vendor:sqlite3",
    ],
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# protocol benchmarks - worker protocol serialization performance
# critical for multi-user nix where all operations go through daemon
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "protocol_bench",
    srcs = ["protocol_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# SSH agent discovery benchmarks - startup overhead for sudo nix operations
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "ssh_bench",
    srcs = ["ssh_bench.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# GC coordination tests - temp roots, zombie reaping, time-based expiry
# Tests for issues #4382, #7572, #8638, #13740
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "gc_test",
    srcs = ["gc_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# Builder health tracking tests - exponential backoff for down remote builders
# Tests for issue #13513
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "builder-health_test",
    srcs = ["builder-health_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# Concurrency/Deadlock tests - thread safety and race condition fixes
# Tests for issues #4216, #6666, #2087, #11979, #9548, #14140, #3695, #14599,
# #1015, #2285
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "concurrency_test",
    srcs = ["concurrency_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)

# ─────────────────────────────────────────────────────────────────────────────
# Daemon crash prevention tests - fixes for crash-inducing bugs
# Tests for issues #14758, #13484, #14300, #14733, #13707, #13844, #12871,
# #12761, #11667, #13721, #6516, #8113, #6065, #11748
# ─────────────────────────────────────────────────────────────────────────────
cxx_test(
    name = "daemon-crash-prevention_test",
    srcs = ["daemon-crash-prevention_test.cpp"],
    deps = STORE_TEST_DEPS,
    compiler_flags = STORE_TEST_COMPILER_FLAGS,
    linker_flags = STORE_LINKER_FLAGS,
    link_style = "static",
)
